# from apps.basic_info.services.create_user import create_user
# from apps.basic_info.services import create_keyword

# def handle_follow_event(event):
#     create_user(event.source.user_id,"測試名稱")

# def handle_message_event(event):
#     create_keyword(event.source.user_id,event.message.text)